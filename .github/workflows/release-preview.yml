name: Release Preview

on:
  pull_request:
    branches: [dev]
  workflow_dispatch:

jobs:
  release_preview:
    uses: OpenVoiceOS/gh-automations/.github/workflows/release-preview.yml@dev
    secrets: inherit
    with:
      package_name: 'HiveMind_matrix_bridge'
      version_file: 'hm_matrix_bridge/version.py'
